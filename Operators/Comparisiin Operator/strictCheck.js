let x = Number(prompt("enter a number\n"))
let y = Number(prompt("enter a number\n"))
let z = String(prompt("enter a number\n"))

if(x===y===z){
    console.log(`All are of same type`);
}
else{
    console.log(`All are not same type`)
}